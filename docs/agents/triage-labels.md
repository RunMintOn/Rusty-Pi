# Triage Labels

Not used in this repo (solo project, no incoming issues). Tickets produced by `/to-tickets` in `tickets.md` are the work breakdown; there is no triage flow.

The standard labels are retained for reference in case the workflow changes:

| Role | Label |
|---|---|
| needs-triage | `needs-triage` |
| needs-info | `needs-info` |
| ready-for-agent | `ready-for-agent` |
| ready-for-human | `ready-for-human` |
| wontfix | `wontfix` |
